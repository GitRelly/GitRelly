print("Welcome to my quiz game")

# creates the option for the user to enter is they want to play.
playing = input("Do you want to play? ")

#creation of a if statement to conditionally check is the player wants to play

#If anything other than the word yes is entered, the program will close.
# The .lower() ensures the answer entered is converted to lowercase and checked if true in the if statement.
if playing.lower() != "Yes":
        print("Okay! let's play :)")
        score = 0
else:   quit()

# An increament of 1 is added to each correct answer.
answer = input("Qhat does WWW stand for? ")
if answer.lower() == "World Wide Web":
    print("Fantastic")
    score += 1
else: print("Incorrect")

answer = input("What does RAM stand for? ")
if answer.lower() == "random access memory":
    print("Fantastic")
    score += 1
else: print("Incorrect")

answer = input("What does GPU stand for? ")
if answer.lower() == "graphics processing unit":
    print("Fantastic your answer was correct")
    score += 1
else: print("Incorrect")

answer = input("What does GUI stand for? ")
if answer.lower() == "graphical user interface":
    print("Fantastic your answer was correct")
    score += 1
else: print("Incorrect")

# prints the concatenated words and convers the score into the string wording telling their score.
print("You got " + str(score) + " questions correct!")
